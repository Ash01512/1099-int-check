{
  "name": "1099-int-check",
  "version": "0.2.0",
  "private": true,
  "description": "Landing page for a free guide that helps extension filers check their 1099-INT interest forms against their IRS Wage & Income Transcript before October 15.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test",
    "dev": "npm run build && wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy",
    "deploy:redirect": "wrangler deploy -c redirect/wrangler.jsonc",
    "tail": "wrangler pages deployment tail --project-name 1099-int-check",
    "verify:csp": "node scripts/verify-csp.mjs https://1099-int-check.pages.dev",
    "setup:delivery": "node scripts/setup-delivery.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "type": "module"
}
